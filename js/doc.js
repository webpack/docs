!function(n){function e(i){if(r[i])return r[i][t];var o=r[i]={exports:{},id:i,loaded:!1};return n[i].call(o[t],o,o[t],e),o.loaded=!0,o[t]}var t="exports",r={},i={0:0};return e.e=function(n,t){if(0===i[n])return t.call(null,e);if(void 0!==i[n])i[n].push(t);else{i[n]=[t];var r=document.getElementsByTagName("head")[0],o=document.createElement("script");o.type="text/javascript",o.charset="utf-8",o.src=e.p+"js/"+n+".js",r.appendChild(o)}},e.modules=n,e.cache=r,e.p="",window.webpackJsonp=function(t,r){for(var o,a,l=[];t.length;)a=t.shift(),i[a]&&l.push.apply(l,i[a]),i[a]=0;for(o in r)n[o]=r[o];for(;l.length;)l.shift().call(null,e)},e(0)}([function(module,exports,require){eval("require(1);\r\n\r\nrequire(3)(function(event) {\r\n	require(9);\r\n});\r\n\n\n// WEBPACK FOOTER\n// module.id = 0\n// module.readableIdentifier = ./app/doc.js\n//@ sourceURL=webpack-module:///./app/doc.js")},function(module,exports,require){eval("if(true) {\r\n	(function(window, document, script, url, r, tag, firstScriptTag) {\r\n		window['GoogleAnalyticsObject']=r;\r\n		window[r] = window[r] || function() {\r\n			(window[r].q = window[r].q || []).push(arguments)\r\n		};\r\n		window[r].l = 1*new Date();\r\n		tag = document.createElement(script),\r\n		firstScriptTag = document.getElementsByTagName(script)[0];\r\n		tag.async = 1;\r\n		tag.src = url;\r\n		firstScriptTag.parentNode.insertBefore(tag, firstScriptTag);\r\n	})(\r\n		window,\r\n		document,\r\n		'script',\r\n		'//www.google-analytics.com/analytics.js',\r\n		'ga'\r\n	);\r\n\r\n	var ga = window.ga;\r\n\r\n	ga('create', (\"UA-46921629-1\"), (\"webpack.github.io\"));\r\n	ga('send', 'pageview');\r\n\r\n	module.exports = function() {\r\n		return window.ga.apply(window.ga, arguments);\r\n	};\r\n} else {\r\n	module.exports = function() {}\r\n}\r\n\n\n// WEBPACK FOOTER\n// module.id = 1\n// module.readableIdentifier = ./app/googleAnalytics.js\n//@ sourceURL=webpack-module:///./app/googleAnalytics.js")},function(module,exports,require){eval('module.exports = function downloadWiki(wiki, callback) {\r\n	var request = new XMLHttpRequest();\r\n	request.open("GET", "http://github-wiki.herokuapp.com/webpack/docs/" + wiki, true);\r\n	request.onreadystatechange = function() {\r\n		if(request.readyState === 4) {\r\n			if(request.status !== 200) {\r\n				return callback(new Error("Statuscode is " + request.status));\r\n			} else {\r\n				return callback(null, request.responseText);\r\n			}\r\n		}\r\n	};\r\n	request.send(null);\r\n};\n\n// WEBPACK FOOTER\n// module.id = 2\n// module.readableIdentifier = ./app/downloadWiki.js\n//@ sourceURL=webpack-module:///./app/downloadWiki.js')},function(module,exports,require){eval("/*!\r\n * contentloaded.js\r\n *\r\n * Author: Diego Perini (diego.perini at gmail.com)\r\n * Summary: cross-browser wrapper for DOMContentLoaded\r\n * Updated: 20101020\r\n * License: MIT\r\n * Version: 1.2\r\n *\r\n * URL:\r\n * http://javascript.nwbox.com/ContentLoaded/\r\n * http://javascript.nwbox.com/ContentLoaded/MIT-LICENSE\r\n *\r\n */\r\n\r\n// @win window reference\r\n// @fn function reference\r\nfunction contentLoaded(win, fn) {\r\n\r\n	var done = false, top = true,\r\n\r\n	doc = win.document, root = doc.documentElement,\r\n\r\n	add = doc.addEventListener ? 'addEventListener' : 'attachEvent',\r\n	rem = doc.addEventListener ? 'removeEventListener' : 'detachEvent',\r\n	pre = doc.addEventListener ? '' : 'on',\r\n\r\n	init = function(e) {\r\n		if (e.type == 'readystatechange' && doc.readyState != 'complete') return;\r\n		(e.type == 'load' ? win : doc)[rem](pre + e.type, init, false);\r\n		if (!done && (done = true)) fn.call(win, e.type || e);\r\n	},\r\n\r\n	poll = function() {\r\n		try { root.doScroll('left'); } catch(e) { setTimeout(poll, 50); return; }\r\n		init('poll');\r\n	};\r\n\r\n	if (doc.readyState == 'complete') fn.call(win, 'lazy');\r\n	else {\r\n		if (doc.createEventObject && root.doScroll) {\r\n			try { top = !win.frameElement; } catch(e) { }\r\n			if (top) poll();\r\n		}\r\n		doc[add](pre + 'DOMContentLoaded', init, false);\r\n		doc[add](pre + 'readystatechange', init, false);\r\n		win[add](pre + 'load', init, false);\r\n	}\r\n\r\n}\r\n\r\nmodule.exports = function(fn) {\r\n	contentLoaded(window, fn);\r\n};\n\n// WEBPACK FOOTER\n// module.id = 3\n// module.readableIdentifier = ./app/onContentLoaded.js\n//@ sourceURL=webpack-module:///./app/onContentLoaded.js")},function(module,exports,require){eval('module.exports = function linkToTitle(link) {\r\n	if(!link) return link;\r\n	return link.toLowerCase().replace(/[^a-z0-9\\.]/g, " ");\r\n}\r\n\n\n// WEBPACK FOOTER\n// module.id = 4\n// module.readableIdentifier = ./lib/linkToTitle.js\n//@ sourceURL=webpack-module:///./lib/linkToTitle.js')},,,,,function(module,exports,require){eval('var linkToTitle = require(4);\r\nvar downloadWiki = require(2);\r\n\r\nvar LRU = require(17);\r\n\r\nvar INTRA_LINK = /^([a-z0-9\\-\\.]+)\\.html$/i;\r\n\r\nfunction bindIntraLinks() {\r\n	document.body.addEventListener("click", function(event) {\r\n		if(event.target.tagName === "A" && INTRA_LINK.test(event.target.getAttribute("href"))) {\r\n			var href = event.target.getAttribute("href");\r\n			INTRA_LINK.lastIndex = 0;\r\n			var wiki = INTRA_LINK.exec(href)[1];\r\n			document.title = linkToTitle(wiki);\r\n			currentPage = wiki;\r\n			history.pushState(null, null, href);\r\n			loadPage(wiki, false);\r\n			event.preventDefault();\r\n		}\r\n	}, false);\r\n}\r\n\r\nfunction highlightIntraLinks() {\r\n	if(document.querySelectorAll) {\r\n		var elements = document.querySelectorAll(\'a[href$=".html"]\');\r\n		for(var i = 0; i < elements.length; i++) {\r\n			var element = elements[i];\r\n			var href = element.getAttribute("href");\r\n			if(element.classList && INTRA_LINK.test(href)) {\r\n				INTRA_LINK.lastIndex = 0;\r\n				var wiki = INTRA_LINK.exec(href)[1];\r\n				if((currentPage === wiki) ^ (element.classList.contains("active"))) {\r\n					element.classList.toggle("active");\r\n				}\r\n			}\r\n		}\r\n	}\r\n}\r\n\r\nvar contentElement = document.getElementById("wiki");\r\nvar titleElement = document.getElementById("wikititle");\r\nvar editElement = document.getElementById("wikieditlink");\r\n\r\nvar pagesCache = LRU({\r\n	max: 10,\r\n	maxAge: 5 * 60 * 1000 // 5m\r\n});\r\nvar currentPage = "";\r\n\r\nwindow.addEventListener("popstate", function() {\r\n	loadCurrentPage(false);\r\n});\r\n\r\nhighlightIntraLinks();\r\nloadCurrentPage(true);\r\n\r\nfunction loadCurrentPage(initial) {\r\n	var match = /\\/([a-z0-9\\-]+)\\.html$/i.exec(location.pathname);\r\n	if(match) {\r\n		var wiki = match[1];\r\n		if(wiki !== currentPage || initial) {\r\n			currentPage = wiki;\r\n			loadPage(wiki, initial);\r\n		}\r\n	}\r\n}\r\n\r\nfunction reportAnalytics() {\r\n	var ga = require(1);\r\n	var location = window.location.protocol +\r\n		\'//\' + window.location.hostname +\r\n		window.location.pathname +\r\n		window.location.search;\r\n	ga(\'set\', \'location\', location);\r\n	ga(\'set\', \'title\', document.title);\r\n	ga(\'send\', \'pageview\');\r\n}\r\n\r\nvar EDIT_LINK = "https://github.com/webpack/docs/wiki/XXX/_edit";\r\n\r\nfunction loadPage(wiki, initial) {\r\n	var cacheEntry = pagesCache.get(wiki);\r\n	if(cacheEntry) {\r\n		editElement.setAttribute("href", EDIT_LINK.replace(/XXX/g, wiki));\r\n		titleElement.innerHTML = linkToTitle(wiki);\r\n		contentElement.innerHTML = cacheEntry;\r\n		if(!initial) reportAnalytics();\r\n		highlightIntraLinks();\r\n		return;\r\n	}\r\n\r\n	if(!initial && document.body.classList) document.body.classList.add("loading");\r\n\r\n	downloadWiki(wiki, function(err, result) {\r\n		if(err) {\r\n			if(initial) {\r\n				if(document.body.classList) document.body.classList.remove("loading");\r\n				return;\r\n			}\r\n			window.location.reload();\r\n		} else {\r\n			require.e/* require */(1, function(require) { var __WEBPACK_AMD_REQUIRE_ARRAY__ = [require(7)]; (function(renderMarkdown) {\r\n				next(renderMarkdown(result));\r\n			}.apply(null, __WEBPACK_AMD_REQUIRE_ARRAY__));});\r\n		}\r\n		function next(result) {\r\n			pagesCache.set(wiki, result);\r\n			if(document.body.classList) document.body.classList.remove("loading");\r\n			editElement.setAttribute("href", EDIT_LINK.replace(/XXX/g, wiki));\r\n			titleElement.innerHTML = linkToTitle(wiki);\r\n			contentElement.innerHTML = result;\r\n			window.scrollTo(0, 0);\r\n			if(!initial) reportAnalytics();\r\n			if(initial) bindIntraLinks();\r\n			highlightIntraLinks();\r\n		}\r\n	});\r\n	require.e/* require */(1/* duplicate */, function(require) {[require(7)];}); // Start loading in parallel\r\n}\n\n// WEBPACK FOOTER\n// module.id = 9\n// module.readableIdentifier = ./app/bindToIntraLinks.js\n//@ sourceURL=webpack-module:///./app/bindToIntraLinks.js')},,,,,,,,function(module,exports,require){eval(';(function () { // closure for web browsers\n\nif (typeof module === \'object\' && module.exports) {\n  module.exports = LRUCache\n} else {\n  // just set the global for non-node platforms.\n  this.LRUCache = LRUCache\n}\n\nfunction hOP (obj, key) {\n  return Object.prototype.hasOwnProperty.call(obj, key)\n}\n\nfunction naiveLength () { return 1 }\n\nfunction LRUCache (options) {\n  if (!(this instanceof LRUCache))\n    return new LRUCache(options)\n\n  if (typeof options === \'number\')\n    options = { max: options }\n\n  if (!options)\n    options = {}\n\n  this._max = options.max\n  // Kind of weird to have a default max of Infinity, but oh well.\n  if (!this._max || !(typeof this._max === "number") || this._max <= 0 )\n    this._max = Infinity\n\n  this._lengthCalculator = options.length || naiveLength\n  if (typeof this._lengthCalculator !== "function")\n    this._lengthCalculator = naiveLength\n\n  this._allowStale = options.stale || false\n  this._maxAge = options.maxAge || null\n  this._dispose = options.dispose\n  this.reset()\n}\n\n// resize the cache when the max changes.\nObject.defineProperty(LRUCache.prototype, "max",\n  { set : function (mL) {\n      if (!mL || !(typeof mL === "number") || mL <= 0 ) mL = Infinity\n      this._max = mL\n      if (this._length > this._max) trim(this)\n    }\n  , get : function () { return this._max }\n  , enumerable : true\n  })\n\n// resize the cache when the lengthCalculator changes.\nObject.defineProperty(LRUCache.prototype, "lengthCalculator",\n  { set : function (lC) {\n      if (typeof lC !== "function") {\n        this._lengthCalculator = naiveLength\n        this._length = this._itemCount\n        for (var key in this._cache) {\n          this._cache[key].length = 1\n        }\n      } else {\n        this._lengthCalculator = lC\n        this._length = 0\n        for (var key in this._cache) {\n          this._cache[key].length = this._lengthCalculator(this._cache[key].value)\n          this._length += this._cache[key].length\n        }\n      }\n\n      if (this._length > this._max) trim(this)\n    }\n  , get : function () { return this._lengthCalculator }\n  , enumerable : true\n  })\n\nObject.defineProperty(LRUCache.prototype, "length",\n  { get : function () { return this._length }\n  , enumerable : true\n  })\n\n\nObject.defineProperty(LRUCache.prototype, "itemCount",\n  { get : function () { return this._itemCount }\n  , enumerable : true\n  })\n\nLRUCache.prototype.forEach = function (fn, thisp) {\n  thisp = thisp || this\n  var i = 0;\n  for (var k = this._mru - 1; k >= 0 && i < this._itemCount; k--) if (this._lruList[k]) {\n    i++\n    var hit = this._lruList[k]\n    if (this._maxAge && (Date.now() - hit.now > this._maxAge)) {\n      del(this, hit)\n      if (!this._allowStale) hit = undefined\n    }\n    if (hit) {\n      fn.call(thisp, hit.value, hit.key, this)\n    }\n  }\n}\n\nLRUCache.prototype.keys = function () {\n  var keys = new Array(this._itemCount)\n  var i = 0\n  for (var k = this._mru - 1; k >= 0 && i < this._itemCount; k--) if (this._lruList[k]) {\n    var hit = this._lruList[k]\n    keys[i++] = hit.key\n  }\n  return keys\n}\n\nLRUCache.prototype.values = function () {\n  var values = new Array(this._itemCount)\n  var i = 0\n  for (var k = this._mru - 1; k >= 0 && i < this._itemCount; k--) if (this._lruList[k]) {\n    var hit = this._lruList[k]\n    values[i++] = hit.value\n  }\n  return values\n}\n\nLRUCache.prototype.reset = function () {\n  if (this._dispose && this._cache) {\n    for (var k in this._cache) {\n      this._dispose(k, this._cache[k].value)\n    }\n  }\n\n  this._cache = Object.create(null) // hash of items by key\n  this._lruList = Object.create(null) // list of items in order of use recency\n  this._mru = 0 // most recently used\n  this._lru = 0 // least recently used\n  this._length = 0 // number of items in the list\n  this._itemCount = 0\n}\n\n// Provided for debugging/dev purposes only. No promises whatsoever that\n// this API stays stable.\nLRUCache.prototype.dump = function () {\n  return this._cache\n}\n\nLRUCache.prototype.dumpLru = function () {\n  return this._lruList\n}\n\nLRUCache.prototype.set = function (key, value) {\n  if (hOP(this._cache, key)) {\n    // dispose of the old one before overwriting\n    if (this._dispose) this._dispose(key, this._cache[key].value)\n    if (this._maxAge) this._cache[key].now = Date.now()\n    this._cache[key].value = value\n    this.get(key)\n    return true\n  }\n\n  var len = this._lengthCalculator(value)\n  var age = this._maxAge ? Date.now() : 0\n  var hit = new Entry(key, value, this._mru++, len, age)\n\n  // oversized objects fall out of cache automatically.\n  if (hit.length > this._max) {\n    if (this._dispose) this._dispose(key, value)\n    return false\n  }\n\n  this._length += hit.length\n  this._lruList[hit.lu] = this._cache[key] = hit\n  this._itemCount ++\n\n  if (this._length > this._max) trim(this)\n  return true\n}\n\nLRUCache.prototype.has = function (key) {\n  if (!hOP(this._cache, key)) return false\n  var hit = this._cache[key]\n  if (this._maxAge && (Date.now() - hit.now > this._maxAge)) {\n    return false\n  }\n  return true\n}\n\nLRUCache.prototype.get = function (key) {\n  return get(this, key, true)\n}\n\nLRUCache.prototype.peek = function (key) {\n  return get(this, key, false)\n}\n\nLRUCache.prototype.pop = function () {\n  var hit = this._lruList[this._lru]\n  del(this, hit)\n  return hit || null\n}\n\nLRUCache.prototype.del = function (key) {\n  del(this, this._cache[key])\n}\n\nfunction get (self, key, doUse) {\n  var hit = self._cache[key]\n  if (hit) {\n    if (self._maxAge && (Date.now() - hit.now > self._maxAge)) {\n      del(self, hit)\n      if (!self._allowStale) hit = undefined\n    } else {\n      if (doUse) use(self, hit)\n    }\n    if (hit) hit = hit.value\n  }\n  return hit\n}\n\nfunction use (self, hit) {\n  shiftLU(self, hit)\n  hit.lu = self._mru ++\n  self._lruList[hit.lu] = hit\n}\n\nfunction trim (self) {\n  while (self._lru < self._mru && self._length > self._max)\n    del(self, self._lruList[self._lru])\n}\n\nfunction shiftLU (self, hit) {\n  delete self._lruList[ hit.lu ]\n  while (self._lru < self._mru && !self._lruList[self._lru]) self._lru ++\n}\n\nfunction del (self, hit) {\n  if (hit) {\n    if (self._dispose) self._dispose(hit.key, hit.value)\n    self._length -= hit.length\n    self._itemCount --\n    delete self._cache[ hit.key ]\n    shiftLU(self, hit)\n  }\n}\n\n// classy, since V8 prefers predictable objects.\nfunction Entry (key, value, lu, length, now) {\n  this.key = key\n  this.value = value\n  this.lu = lu\n  this.length = length\n  this.now = now\n}\n\n})()\n\n\n// WEBPACK FOOTER\n// module.id = 17\n// module.readableIdentifier = ./~/lru-cache/lib/lru-cache.js\n//@ sourceURL=webpack-module:///./~/lru-cache/lib/lru-cache.js')}]);